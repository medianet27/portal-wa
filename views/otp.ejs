<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verifikasi OTP | Portal ISP</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light d-flex align-items-center" style="min-height: 100vh;">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-12 col-sm-8 col-md-6 col-lg-4">
        <div class="card shadow mt-5">
          <div class="card-body p-4">
            <div class="text-center mb-4">
              <img src="/img/logo.png" alt="Logo" style="max-width:80px;">
              <h4 class="mt-2 mb-0">Verifikasi OTP</h4>
              <small class="text-muted">Masukkan kode OTP yang dikirim ke WhatsApp Anda</small>
            </div>
            <% if (error) { %>
              <div class="alert alert-danger py-2"><%= error %></div>
            <% } %>
            <form method="POST" action="/customer/otp">
              <input type="hidden" name="phone" value="<%= phone %>">
              <div class="mb-3">
                <label for="otp" class="form-label">Kode OTP</label>
                <input type="text" class="form-control text-center" id="otp" name="otp" placeholder="6 digit" required pattern="\d{6}" maxlength="6" autofocus>
              </div>
              <button type="submit" class="btn btn-primary w-100">Verifikasi</button>
            </form>
            <div class="mt-3 text-center">
              <small class="text-muted">&copy; <%= new Date().getFullYear() %> ALIJAYA DIGITAL NETWORK</small>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html> 